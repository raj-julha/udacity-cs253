<!DOCTYPE html>
<html>
    <head>
        <title>Raj Julha - Udacity CS253 Login (Homework 4-2)</title>
    </head>
    <body>
        <h1>Login</h1>
        <form method="post">
            <label>
                <div>Username</div>
                <input type="text" name="username" width="80" value="{{username}}" >
            </label>
            <label>
                <div>Password</div>
                <input type="text" name="password" width="80" value="{{password}}" >
            </label>

            <div class="error">{{error}}</div>
            <input type="submit">
        </form>
    </body>
</html>


